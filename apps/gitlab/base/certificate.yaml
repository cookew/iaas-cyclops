apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab
spec:
  commonName: gitlab
  dnsNames:
  - "gitlab.apps.iaas.wcooke.me"
  - "*.gitlab.apps.iaas.wcooke.me"
  - "*.pages.gitlab.apps.iaas.wcooke.me"
  duration: 960h
  isCA: false
  issuerRef:
    kind: ClusterIssuer
    name: iaas-wcooke-me
  secretName: gitlab-certificate
  usages:
  - server auth
  - client auth
