apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: pulp
spec:
  bucketName: pulp
  storageClassName: ceph-bucket
  additionalConfig:
    maxObjects: "1000000"
    maxSize: "50G"
    bucketPolicy: |
      {
        "Version": "2012-10-17",
        "Statement": [
          {
            "Sid": "Statement1",
            "Effect": "Allow",
            "Principal": {
              "AWS": [
                "arn:aws:iam:::user/pulp"
              ]
            },
            "Action": "s3:*",
            "Resource": "*"
          }
        ]
      }
