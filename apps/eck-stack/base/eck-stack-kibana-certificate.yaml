apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kibana-http-certificate
spec:
  commonName: kibana-http-certificate
  dnsNames:
    - kibana.apps.iaas.wcooke.me
    - kibana.eck-stack.svc
  duration: 960h
  isCA: false
  issuerRef:
    kind: ClusterIssuer
    name: iaas-wcooke-me
  secretName: kibana-http-certificate
  usages:
  - client auth
  - server auth
