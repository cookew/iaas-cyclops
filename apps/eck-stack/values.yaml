# https://github.com/elastic/cloud-on-k8s/blob/main/deploy/eck-stack/values.yaml

eck-agent:
  enabled: false

eck-apm-server:
  enabled: true

eck-autoops-agent-policy:
  enabled: false

eck-beats:
  enabled: false

eck-elasticsearch:
  enabled: true
  fullnameOverride: elasticsearch

eck-fleet-server:
  enabled: true
  deployment:
    replicas: 2
    podTemplate:
      spec:
        serviceAccountName: fleet-server
        automountServiceAccountToken: true
  revisionHistoryLimit: 1

eck-kibana:
  config:
    csp.strict: true
    server.securityResponseHeaders.disableEmbedding: true
    server.securityResponseHeaders.strictTransportSecurity: "max-age=31536000"
    server.ssl.certificate: /kibana-http-certs/tls.crt
    server.ssl.key: /kibana-http-certs/tls.key
    xpack.security.session.idleTimeout: "15m"
    xpack.security.session.lifespan: "1d"
    xpack.security.session.concurrentSessions:
      maxSessions: 5
  elasticsearchRef:
    name: elasticsearch
  enabled: true
  # ref: https://www.elastic.co/guide/en/cloud-on-k8s/current/k8s-stack-monitoring.html
  ingress:
    enabled: true
    annotations:
      cert-manager.io/cluster-issuer: iaas-wcooke-me
      forecastle.stakater.com/appName: Kibana
      forecastle.stakater.com/expose: "true"
      forecastle.stakater.com/group: Apps
      forecastle.stakater.com/icon: https://kibana.apps.iaas.wcooke.me/logo.png
      forecastle.stakater.com/network-restricted: "true"
      ingress.cilium.io/tls-passthrough: enabled
    className: cilium
    hosts:
      - host: kibana.apps.iaas.wcooke.me
        path: /
    labels: {}
    pathType: ImplementationSpecific
    tls:
      enabled: true
      secretName: kibana-ingress-tls
  monitoring: {}
    # metrics:
    #   elasticsearchRefs:
    #   - name: monitoring
    #     namespace: observability
    # logs:
    #   elasticsearchRefs:
    #   - name: monitoring
    #     namespace: observability
  podTemplate:
    spec:
      volumes:
      - name: kibana-http-certs
        secret:
          secretName: kibana-http-certificate
      containers:
      - name: kibana
        volumeMounts:
        - name: kibana-http-certs
          mountPath: /kibana-http-certs
          readOnly: true
  revisionHistoryLimit: 1
  secureSettings: []

eck-logstash:
  enabled: true

eck-package-registry:
  enabled: true
