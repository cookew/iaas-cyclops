apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- base/eck-operator-webhook-cert.yaml

helmCharts:
- name: eck-operator
  includeCRDs: true
  namespace: eck-operator
  releaseName: eck-operator
  repo: https://helm.elastic.co
  valuesFile: values.yaml
  version: 3.4.1
