apiVersion: keycloak.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
  name: keycloak-iaas
spec:
  credentials:
    secretRef:
      key: credentials
      name: provider-keycloak-credentials
      namespace: crossplane-provider-keycloak
    source: Secret
