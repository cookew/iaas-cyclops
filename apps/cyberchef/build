#!/usr/bin/bash -e

REGISTRY=registry.wcooke.me:5000
REPO=cyberchef
VERSION=10.19.4

docker build -t ${REGISTRY}/${REPO}:${VERSION} .
docker image push ${REGISTRY}/${REPO}:${VERSION}

sed -i "s@- image: ${REGISTRY}/${REPO}:.*@- image: ${REGISTRY}/${REPO}:${VERSION}@g" base/deployment.yml
