apiVersion: oss.grafana.crossplane.io/v1alpha1
kind: User
metadata:
  name: ceph_dashboard
spec:
  forProvider:
    login: ceph_dashboardabcd
    name: ceph_dashboardabcd
    passwordSecretRef:
      key: password
      name: grafana-ceph-dashboard-user
  providerConfigRef:
    name: provider-grafana
