# apiVersion: objectbucket.io/v1alpha1
# kind: ObjectBucketClaim
# metadata:
#   name: guacamole-pg-backup
# spec:
#   bucketName: guacamole-pg-backup
#   storageClassName: ceph-bucket
#   additionalConfig:
#     maxObjects: "1000000"
#     maxSize: "10G"
# ---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: guacamole-pg-cluster
spec:
  affinity:
    topologyKey: kubernetes.io/hostname
  bootstrap:
    initdb:
      database: guacamole
  #     encoding: UTF8
  #     localeCType: C
  #     localeCollate: C
  #     owner: guacamole
  # enablePDB: true
  # enableSuperuserAccess: false
  imageCatalogRef:
    apiGroup: postgresql.cnpg.io
    kind: ClusterImageCatalog
    major: 18
    name: postgresql-standard-trixie
  instances: 3
  monitoring:
    enablePodMonitor: true
  # plugins:
  # - name: barman-cloud.cloudnative-pg.io
  #   isWALArchiver: true
  #   parameters:
  #     barmanObjectName: guacamole-pg
  primaryUpdateMethod: switchover
  storage:
    resizeInUseVolumes: true
    size: 1Gi
    storageClass: ceph-filesystem
  walStorage:
    resizeInUseVolumes: true
    size: 3Gi
    storageClass: ceph-filesystem
# ---
# apiVersion: barmancloud.cnpg.io/v1
# kind: ObjectStore
# metadata:
#   name: guacamole-pg
# spec:
#   configuration:
#     data:
#       compression: bzip2
#     destinationPath: s3://guacamole-pg-backup
#     endpointCA:
#       key: ca.crt
#       name: guacamole-ca-bundle
#     endpointURL: https://rook-ceph-rgw-ceph-objectstore.rook-ceph-cluster.svc
#     s3Credentials:
#       accessKeyId:
#         key: AWS_ACCESS_KEY_ID
#         name: guacamole-pg-backup
#       inheritFromIAMRole: false
#       secretAccessKey:
#         key: AWS_SECRET_ACCESS_KEY
#         name: guacamole-pg-backup
#     wal:
#       compression: bzip2
#   retentionPolicy: 90d
# ---
# apiVersion: postgresql.cnpg.io/v1
# kind: ScheduledBackup
# metadata:
#   name: guacamole-pg
# spec:
#   backupOwnerReference: cluster
#   cluster:
#     name: guacamole-pg-cluster
#   method: plugin
#   pluginConfiguration:
#     name: barman-cloud.cloudnative-pg.io
#   schedule: "@daily"
#   target: prefer-standby
