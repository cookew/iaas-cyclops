apiVersion: grafana.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
  name: grafana-provider-config
spec:
  credentials:
    secretRef:
      key: credentials
      name: grafana-credentials
      namespace: crossplane-provider-grafana
    source: Secret
